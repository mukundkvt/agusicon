# OS / IDE
.DS_Store
Thumbs.db

# Claude / project meta
.agents/
.claude/
skills-lock.json
CLAUDE.md
docs/

# Deploy script (contains credentials — local only)
deploy.py

# Archives (large, not needed in git)
*.zip

# Live data — CSV logs, uploads (managed on server, not committed)
data/*.csv
data/uploads/
data/registration-sequence-*.txt
pages/uploads/
pages/registration-sequence-*.txt
pages/registrationleads.csv
pages/registration-payments.csv
pages/registrations-combined.csv
